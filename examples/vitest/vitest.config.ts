import { defineConfig } from 'vitest/config'
import { type Plugin } from 'vite'

export const Factorio = (options: any): Plugin => {
  return {
    name: 'vite-plugin-vitest-factorio',
    config: (config) => {
      const disconnect =
    }
  }
}

export default defineConfig({
  plugins: [],
})
